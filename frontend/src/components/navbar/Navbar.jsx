import React from "react";
import LoadingSpinner, { Spinner } from "../shared/Spinner";

const Navbar = () => {
  return (
    <div>
      Navbar
      <Spinner />
    </div>
  );
};

export default Navbar;
